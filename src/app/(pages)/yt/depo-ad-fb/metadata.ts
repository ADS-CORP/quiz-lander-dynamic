import { generatePageMetadata } from '@/utils/metadata';
import { yourTruthBrand } from '@/config/brands/yt';

export const metadata = generatePageMetadata(
  'Depo-Provera Compensation',
  'Did you suffer bone density loss or other serious side effects after using Depo-Provera? You may be eligible for significant compensation. Free case review available.',
  yourTruthBrand
);