import { generatePageMetadata } from '@/utils/metadata';
import { peoplesJusticeBrand } from '@/config/brands/pj';

export const metadata = generatePageMetadata(
  'Hair Relaxer Compensation',
  'Were you diagnosed with cancer after using hair relaxers? You may be eligible for significant compensation. Free case review available.',
  peoplesJusticeBrand
);